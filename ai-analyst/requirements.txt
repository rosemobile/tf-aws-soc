# AI Alert Analyst Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
requests==2.32.4
pyyaml>=6.0

# LLM Providers (install based on your choice)
openai==1.54.3
anthropic>=0.8.0

# Optional: Local LLM support
# ollama (no pip package, install separately)

# Optional: Enhanced features
python-dateutil>=2.8.0
rich>=13.0.0  # Better terminal formatting

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
mypy>=1.0.0

h11==0.16.0
urllib3==2.6.3